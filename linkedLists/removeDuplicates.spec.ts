import { removeDuplicatesFromLinkedList } from './removeDuplicates'
import { SingleLinkedList } from '../types'

describe('LinkedList', () => {
  it('Test Case #1', () => {
    const input = new SingleLinkedList(1)
  })
})
